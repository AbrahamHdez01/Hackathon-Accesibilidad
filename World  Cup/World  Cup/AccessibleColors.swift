import SwiftUI

extension Color {
    // Paleta para la pantalla de rutas accesibles (alta legibilidad)
    static let routeDarkGreen = Color(red: 0.06, green: 0.21, blue: 0.19) // fondo
    static let routePanelGreen = Color(red: 0.10, green: 0.28, blue: 0.25) // cards/paneles
    static let accessibleLime = Color(red: 0.66, green: 1.0, blue: 0.24)   // #A8FF3D
    static let routeText = Color.white
    static let routeMuted = Color.white.opacity(0.75)
}


